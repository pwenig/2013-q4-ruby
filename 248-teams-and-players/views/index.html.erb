<h1>List of teams</h1>

<ul>
  <% @teams.each do |team| %>
    <li>
      <a href="/teams/<%= team.name %>">
        <%= team.name %>
      </a>
      consists of
      <% Player.where(team_id: team.id).each do |player| %>
      <%= player.name %>
      <% end %>
    </li>

  <% end %>
</ul>
