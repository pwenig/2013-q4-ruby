<%= error_messages_for @patient %>      <%# this prints the error messages for @patent %>

<a href="/">Back</a>                <%# intiates the / route %>

<% if @patient.id == nil %>       <%# it would be nill if this is a new record %>
  <h1>Add new patient</h1>        <%# prints h1 title Add New Patient %>
<% else %>
  <h1>Edit patient #<%= @patient.id %></h1>
<% end %>

<p>
  <%= label_tag "systolic" %>
  <br>
  <%= text_field_tag "systolic", @patient.systolic %>
</p>

<p>
  <%= label_tag "diastolic" %>
  <br>
  <%= text_field_tag "diastolic", @patient.diastolic %>
</p>

<p>
  <% if @patient.id == nil %>      
    <%= submit_tag "Add" %>
  <% else %>
    <%= submit_tag "Update" %>
    <%= submit_tag "Delete" %>
  <% end %>
</p>
