<h1>List of players</h1>

<ul>
  <% @players.each do |player| %>
    <li>
      <a href="/players/<%= player.id %>">
        <%= player.name %>
      </a>

      is on the
      <%= player.team.name  %>	<!-- player's team's name -->
      team


    </li>
  <% end %>
</ul>
